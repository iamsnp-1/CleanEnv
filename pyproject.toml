[project]
name = "datacleanenv-x"
version = "1.0.0"
description = "OpenEnv-compliant data cleaning simulation environment"
requires-python = ">=3.10"

dependencies = [
    "pandas",
    "numpy",
    "pydantic",
    "openai",
    "fastapi",
    "uvicorn[standard]",
    "huggingface_hub",
]
